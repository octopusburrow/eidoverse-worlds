# dependencies
node_modules/
**/node_modules/

# secrets & live tokens — NEVER commit
.sessions.json
mcpl/tokens.json
mcpl/state.json
tools/voicebot/voices.json
.env
.env.*
deploy/IDENTITY.md

# runtime world data (logs are chat history; poses/snapshots are live state)
worlds/
worlds-dev/

# the asset library is fetched at runtime (EIDOVERSE_DIR), not vendored
assets/
*.glb
*.vrm
*.hdr

# generated
assets/opt/thumbs/
*.zip

# local claude/mcp config
.mcp.json
.memsearch/
tools/node_modules/

# Voice models are 60 MB binaries — they belong on disk, not in git.
client/voices/*.onnx
